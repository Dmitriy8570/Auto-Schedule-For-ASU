# Вопросы для написания дипломной работы

> Дмитрий, ответь, пожалуйста, прямо в этом файле под каждым вопросом (можно кратко).
> Где не знаешь / не важно — так и пиши «не знаю» или «на твоё усмотрение».
> Чем подробнее ответишь на блоки 2–5, тем меньше мне придётся выдумывать и тем легче будет защищаться.

---

## 0. Самое срочное (расхождения в документах)

**0.1.** Точная формулировка темы. В документах три разных варианта:
- «Разработка веб-приложения для автоматизированного формирования учебного расписания АлтГУ» (из «Информация о проекте»),
- «РАЗРАБОТКА ВЕБ-ПРИЛОЖЕНИЯ ДЛЯ АВТОМАТИЗИРОВАННОГО ФОРМИРОВАНИЯ УЧЕБНОГО РАСПИСАНИЯ УНИВЕРСИТЕТА» (заголовок в `Docs/диплом.docx`),
- «Бэкенд-разработка веб-приложения для автоматизированного формирования учебного расписания университета» (текст к защите).

Какая формулировка **утверждённая в приказе** (точно, буква в букву)? Должно ли в теме фигурировать слово «бэкенд / серверная часть»?

Ответ: 1 вариант, общее название.

**0.2.** Титульный лист `титул_и_последний лист.docx` содержит чужие данные: «Шацких Алексей Евгеньевич», тема «Разработка backend-части информационной системы управления мероприятиями». Это просто шаблон, который надо переделать под тебя? Подтверди свои данные для титула:
- ФИО полностью: Бардаков Дмитрий Александрович — верно?
- Группа: 4.205-2 — верно?
- Научный руководитель: Михеева Татьяна Викторовна, к.т.н., доцент — верно?
- Зав. кафедрой: Козлов Денис Юрьевич (в одном файле к.ф.-м.н., в другом к.т.н. — как правильно?)
- Председатель ГЭК: Леонов Сергей Леонидович, д.т.н., профессор — верно?
- Год защиты: 2026, город Барнаул — верно?

Ответ: В титул_и_последний лист.docx это пример, тебе необходимо перенести всё отсюда, в файл диплом.docx, изменить только ФИО и название дипломной.

**0.3.** Главный вопрос про объём работы. В черновике Глава 1 уже описывает систему «по максимуму» (интеграция с ММИС, ролевая модель, real-time, 7 жёстких + 3 мягких ограничения). В коде же на данный момент реализована меньшая часть. Как писать Главы 2 и 3?
- (а) Описывать **спроектированную** систему целиком (как замысел/архитектуру), а в реализации честно отметить, что часть — задел/в разработке;
- (б) Описывать **строго то, что реально работает в коде**, не заявляя лишнего;
- (в) Промежуточный вариант: проектирование — полное (как задумано), реализация (Глава 3) — только готовое, с разделом «направления развития».

Что выбираешь? (Это критично — на защите спросят «покажите, что работает».)

Ответ: Смотри, я буду параллельно с дипломом, доделывать проект, поэтому диплом пишется так, как будто проект полностью готов.

---

## 1. Формальные требования и логистика

**1.1.** В каком формате тебе нужен итог: я наполняю существующий `Docs/диплом.docx`, или делаю отдельный новый `.docx` в папке `Диплом`? (Я не могу сам красиво форматировать .docx по ГОСТ — могу выдать готовый текст, который ты вставишь, либо собрать .docx скриптом. Как удобнее?)

Ответ: Мы вместе с тобой работает на файлом диплом.docx в корне каталога, я буду писать диплом и при необходимости просить тебя что либо написать, отредактировать, изменить и тд.

**1.2.** Целевой объём — 60 страниц (как в реферате)? Жёсткие требования вуза: 40–80 страниц. Тебе хватит 60, или нужно больше/меньше?

Ответ: Целевой объём не менее 60 страниц. То есть минимальный порог, чем больше тем лучше, в разумных обьемах

**1.3.** Дедлайн сдачи на нормоконтроль / предзащиту / защиту? (чтобы понимать темп)

Ответ: Дедлайн тебе знать не обязательно, я буду тобой пользоваться только для помощи.

**1.4.** Антиплагиат: какой требуется процент оригинальности и какая система (Антиплагиат.ВУЗ?)? Это влияет на то, насколько «своими словами» писать.

Ответ: не знаю.

**1.5.** Нужны ли акты внедрения / справка от бюро расписаний АлтГУ? Есть ли возможность их получить?

Ответ: пока что не нужно, но хороших совет на будущее. 

---

## 2. Предметная область и разделение труда в команде

**2.1.** Команда из 2 человек. Подтверди границу: **ты — весь бэкенд** (домен, БД, алгоритм OR-Tools, API), **товарищ — фронтенд (Vue/TS) и Docker**. Верно? Кто именно делал docker-compose и развёртывание?

Ответ: всё верно, докером занимался мой товарищ

**2.2.** В черновике есть раздел «3.5. Контейнеризация и развёртывание с помощью Docker». Если Docker делал товарищ — оставляем этот раздел в твоей работе (как часть общей системы, с оговоркой) или убираем/сокращаем?

Ответ: этот раздел убираем, не обращай внимание на тот диплом, он не актуален

**2.3.** Как у вас с товарищем поделены темы ВКР, чтобы не было пересечения текста (антиплагиат между вами)? Какая у него тема и что он точно описывает у себя?

Ответ: Тема дипломной работы у нас одна, но текст будет совершенно разный, я описываю всё что связано с бэкендом, кроме докера. То есть у нас будет одинаковая только актуальность, всё остальное разное

**2.4.** Поход в бюро расписаний: что конкретно вам рассказали? Уточни детали, которых нет в черновике:
- Сколько человек в бюро расписаний?
- Сколько примерно занятий/пар в семестр они расставляют? Сколько групп, преподавателей, аудиторий, корпусов в АлтГУ (хотя бы порядок)?
- Сколько по времени реально занимает один цикл составления (в черновике встречается и «3 дня», и «несколько недель» — что верно)?
- Какие у них самые болезненные проблемы (с их слов)?

Ответ: там 7 человек, за каждым человеком свои институты. Изначально они составляют расписание на весь семестр вперед, дальше они его корректируют. Сколько по времени занимает не узнавали. Самые болезненными проблемами является, одновременная работа, пересечения приходится отслеживать вручную, измененное расписание публикуция только через 3 дня, постоянно приходится проверять нагрузку преподавателей, потому что у них нету сервиса который уведоваляет об изменении нагрузки. Всё происходит с помощью присланых документов и звонков на внутренний телефон.

**2.5.** Система «Нагрузка ВУЗа» (ММИС): в каком виде оттуда берутся данные (выгрузка Excel? БД? API?)? Реально ли вы планируете интеграцию, или это пока «на будущее»?

Ответ: В университете есть нагрузкаВуза, пока что не реализован этот модуль, но в будущем планируется выгрузка с бд, которая есть в университете, с переодичностью либо сутки либо 3 часа, еще не определились, в дипломе нужно писать что это реализовано.

**2.6.** Попытка перехода на «1С:Университет»: это точно был «1С:Университет» или «1С:Автоматизированное составление расписания. Университет»? Чем именно он им не подошёл (своими словами, что говорили сотрудники)?

Ответ: 1С:Автоматизированное составление расписания. Университет. Он не удобный а также при одновременном сохранении расписания происходят накладки. Также при собственном изучении, он не позволяет создавать специфичные правила составления расписания.

---

## 3. Архитектура, БД и доменная модель (для Главы 2)

**3.1.** Подтверди слои Clean Architecture, как в коде: **Domain → Application → Infrastructure → API**. Используете ли вы CQRS/MediatR (в коде вижу MediatR, Commands/Queries) и FluentValidation — это надо описать как осознанный выбор?

Ответ: Это всё осознаный выбор, и связан он с лучшими подходами к проектированию приложений. Можешь тут выдумать еще что нибудь, может это натолкнет меня реализовать в проекте.

**3.2.** База данных. В коде сейчас в `DbContext` подключены только `Lessons, Groups, Teachers, Classrooms`, есть 2 миграции (`Init`, `InitTest`). При этом доменных сущностей гораздо больше (Curriculum, Workload, Semester/Week, Building, Institute, Department, Course, Degree, Stream, Subject, Equipment, Availability, Log и т.д.). Что считать «итоговой моделью БД» для диплома — полную доменную модель (как задумана) или то, что реально в миграциях? Есть ли у тебя ER-диаграмма (вижу `Docs/Diagram_db.drawio`) — она актуальна?

Ответ: ER-диаграмма актуальна, если будут изменинения, я изменю

**3.3.** Логирование изменений нагрузки (сущности `Log`, `WeekLog`, `SemesterLog`): это реализовано/работает, или пока только сущности? Насколько важно описывать «историю изменений нагрузки» как фичу?

Ответ: история изменений нагрузки это фича приложений. Все логи будут реализованы.

**3.4.** Версионирование расписания (в `Lesson` есть `ScheduleVersion: Current/Draft`): это используется? Опиши задумку — черновик расписания vs опубликованное?

Ответ: при составлении расписания, дисциплины которые они создают, помечаются как draft, при нажатии на кнопку сохранить, они публикуются и становятся current. Также можно отменить все измениния и тогда все сущности пользователя с draft удалятся

**3.5.** Транзакции с уровнем изоляции SERIALIZABLE (заявлено в речи к защите как решение проблемы коллизий 1С). Это реально настроено в коде/EF, или это пока проектное решение «как надо»? (Я в коде явной настройки изоляции пока не нашёл — уточни.)

Ответ: Опять же это всё будет, нужно писать что реализовано.

**3.6.** Real-time синхронизация между пользователями (заявлена в требованиях). Технология — SignalR / WebSocket / что-то ещё? Реализовано или план?

Ответ: На твое усмотрение.

**3.7.** Аутентификация и ролевая модель (администратор + сотрудник бюро). Реализована ли (JWT / Identity)? Или пока только в требованиях?

Ответ: На твое усмотрение, но будет модуль, который будет аутентифицировать пользователей из домена. В университете у всех есть доменные учетные записи.

---

## 4. Алгоритм автоматического составления (ядро, для Глав 2–3)

> Это самая ценная и «защищаемая» часть. По коду я вижу реально реализованными: создание булевых переменных `Lessons[нагрузка, аудитория, слот]`, запрет пересечений (аудитория/преподаватель/группа), лимит пар в день (жёстко: преп. ≤7, группа ≤4), точное соответствие часов за семестр (`сумма == Hours/2`), и мягкий штраф за «окна» (студентов и преподавателей). Хочу свериться с тобой.

**4.1.** Подтверди модель переменных: трёхмерный массив `[нагрузка(SemesterWorkload) × аудитория × временной слот]` булевых переменных, решатель CP-SAT. Верно? Это твоя итоговая модель или менялась?

Ответ: На твое усмотрение. Но скорее всего верно.

**4.2.** Какие ограничения **сейчас реально работают** (запускается и даёт результат)? Отметь по списку: пересечения / часы за семестр / лимит пар в день / окна. Что-то ещё?

Ответ: Должны работать все.

**4.3.** Какие ограничения **спроектированы, но ещё не реализованы** в солвере (есть доменные поля, но нет builder'а)? По коду это: доступность преподавателя/аудитории, оборудование, смена группы, перемещение между корпусами, двойная пара (`Double`), параллельность подгрупп (`Parallelism`), предпочитаемый корпус (`FavoriteBuilding`). Что из этого ты успеешь доделать до защиты, а что честно отнести в «дальнейшее развитие»?

Ответ: Я всё успею реализовать.

**4.4.** В `DailyLessonsLimitSectionBuilder` стоит комментарий «НЕОБХОДИМО ИЗМЕНИТЬ НА СИСТЕМУ ШТРАФОВ» и лимиты захардкожены (7 и 4). Лимит пар в день — это жёсткое или мягкое ограничение в итоговой версии? Откуда берётся число (нормативы)?

Ответ: Будет мягкое ограничение, но с большим штрафом, потому что хоть так нельзя, но бывают ситуации когда необходимо поставить больше 4 пар на группу.

**4.5.** Целевая функция: сейчас минимизируется сумма штрафов за окна (веса `TeacherGap`, `StudentGap` из `ConstraintConfig`). Кто задаёт веса штрафов — администратор через настройки, или они зашиты? Какие ещё слагаемые планируются?

Ответ: Пока что они зашиты, но в будущем можно будет добавить администратора который будет самостоятельно корректировать штрафы. Остальные слагаемые можешь подумать сам.

**4.6.** Есть ли уже **запуск солвера end-to-end** (данные из БД → модель → решение → сохранение Lesson'ов)? Или пока солвер тестируется на синтетических данных в тестах (`ScheduleVisualizationTests`)? Опиши, как ты его прогонял и что получал.

Ответ: Это пока что не важно.

**4.7.** Какие результаты прогона можешь показать в Главе 3 (метрики)? Например: размер задачи (N нагрузок, M аудиторий, T слотов), время решения, число переменных, статус решателя (OPTIMAL/FEASIBLE), число «окон». Есть ли реальные цифры или нужно сгенерировать тестовый набор и измерить?

Ответ: это пока что не важно.

**4.8.** Почему именно OR-Tools / CP-SAT, а не генетика / ЦЛП / своя эвристика? Пробовал ли что-то ещё, или выбор был сразу? (для обоснования в тексте)

Ответ:на твое усмотрение.

---

## 5. API, тестирование, развёртывание (для Главы 3)

**5.1.** API сейчас: вижу один эндпоинт `GET /api/lessons/{id}` и запросы Get by Group/Room/Teacher в Application. Какой набор эндпоинтов планируется к защите (CRUD занятий, запуск генерации, управление аудиториями, нагрузка)? Перечисли, что реально будет.

Ответ: Это пока что не трогаем, в процессе доделывания напишу.

**5.2.** Управление аудиторным фондом (раздел 3.4 черновика): реализован CRUD аудиторий? Или пока сущности `Classroom/Building/Equipment` без API?

Ответ: Это пока что не трогаем, в процессе доделывания напишу.

**5.3.** Тестирование. В репозитории вижу `Tests/ConstraintBuilderTests.cs` и `ScheduleVisualizationTests.cs` (xUnit). Что они реально проверяют? Есть ли интеграционные тесты (БД + API)? Какое примерно покрытие? В речи к защите заявлены модульные + интеграционные — насколько это соответствует факту?

Ответ:Это пока что не трогаем, в процессе доделывания напишу.

**5.4.** Документация API: используется Swagger/OpenAPI? Нужно ли показывать в работе?

Ответ: Это пока что не трогаем, в процессе доделывания напишу.

**5.5.** Развёртывание: где планируется крутить (сервер вуза / облако / локально)? docker-compose поднимает: API + PostgreSQL + фронтенд? Это работает целиком сейчас?

Ответ: Это пока что не трогаем, в процессе доделывания напишу.

---

## 6. Содержательные детали для текста

**6.1.** Новизна/уникальность твоей работы — что подчёркивать? (адаптация под регламенты АлтГУ, расширяемая архитектура ограничений через builder'ы, интеграция с ММИС — что из этого главное?)

Ответ: по твоему усмотрению.

**6.2.** Список литературы: нужно 30+ источников по ГОСТ. У тебя уже есть подобранные источники, или мне предложить список (OR-Tools, CP, теория расписаний, Clean Architecture, .NET, PostgreSQL, статьи ITC-2007/2019)? Какие источники ты реально читал/использовал?

Ответ: не знаю

**6.3.** Диаграммы/рисунки (заявлено 9 рисунков, 5 таблиц). Какие у тебя уже есть в готовом виде (ER-диаграмма, схема архитектуры, схема классов builder'ов)? Какие надо нарисовать? В чём рисуешь (drawio)?

Ответ: На твое усмотрение

**6.4.** Скриншоты интерфейса для приложений: есть ли работающий UI у товарища, чтобы вставить скриншоты, или работа чисто серверная и UI не показываем?

Ответ: Это пока что не трогаем, в процессе доделывания напишу.

**6.5.** Стиль изложения: строго академически-сухо или допускается чуть живее? Есть ли у научного руководителя замечания к уже написанной Главе 1, которые надо учесть?

Ответ: Старый диплом не трогаем.

---

## 7. Свободное поле

Допиши всё, что я не спросил, но что важно знать о проекте, о требованиях кафедры, о том, что уже обсуждалось с руководителем, и о том, на чём хочешь сделать акцент на защите.

Ответ: Сильно не ориентируйся на старый диплом, он не совсем актуален. Мы пишем совершенно новый диплом. 
