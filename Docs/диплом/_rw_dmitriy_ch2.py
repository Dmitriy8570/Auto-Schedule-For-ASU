# -*- coding: utf-8 -*-
"""Переписывает Главу 2 в Диплом.docx: естественнее тон, конкретика реального кода,
структура ближе к методичке. Рисунки 2.1-2.7 переиспользуются из _figs/. Глава 1 не трогается."""
import glob
from docx import Document
from docx.oxml.ns import qn
from docx.text.paragraph import Paragraph
import _docxlib as L

doc = Document("Диплом.docx")
body = doc.element.body


def find(prefix):
    for el in list(body):
        if el.tag == qn('w:p') and Paragraph(el, doc).text.strip().startswith(prefix):
            return el
    raise SystemExit("not found: " + prefix)


el_ch2 = find("2 ПРОЕКТИРОВАНИЕ")
el_ch3 = find("3 РЕАЛИЗАЦИЯ")

# Удаляем старую главу 2: всё от заголовка главы 2 до (не включая) главы 3.
rm, started = [], False
for el in list(body):
    if el is el_ch2:
        started = True
    if el is el_ch3:
        break
    if started:
        rm.append(el)
for el in rm:
    body.remove(el)

# Запоминаем число элementов ДО генерации (новые добавятся в конец, перед w:sectPr).
n_before = len(list(body))


def fig(pat):
    return sorted(glob.glob("_figs/dmitriy_%s*.png" % pat))[0]


# ---------------- НОВАЯ ГЛАВА 2 ----------------
L.chapter(doc, "2 ПРОЕКТИРОВАНИЕ СЕРВЕРНОЙ ЧАСТИ СИСТЕМЫ")
L.body(doc, "Опираясь на результаты анализа предметной области и выбор метода решения, "
    "сделанные в первой главе, в этой главе проектируется серверная часть системы. Сначала "
    "формулируются требования к ней и обосновывается технологический стек, затем описывается "
    "многоуровневая архитектура и модель данных, после чего строится математическая модель "
    "задачи и проектируется модуль автоматического составления расписания. Завершают главу "
    "проектные решения, отвечающие за надёжную совместную работу: версионирование расписания, "
    "транзакционная защита от коллизий, журналирование нагрузки, интеграция с системой "
    "«Нагрузка ВУЗа», программный интерфейс, аутентификация и уведомления реального времени.")

L.heading2(doc, "2.1 Требования к серверной части системы")
L.body(doc, "Серверная часть несёт на себе доменную модель, хранение данных, алгоритмическое ядро "
    "и программный интерфейс для клиентского приложения. Перечень функциональных требований к ней "
    "вытекает напрямую из проблем бюро расписаний, разобранных в разделе 1.2.")
L.bullet(doc, "хранить нормализованные данные об аудиторном фонде, учебной нагрузке и расписании;")
L.bullet(doc, "получать справочники и нагрузку из системы «Нагрузка ВУЗа» с фиксацией истории изменений;")
L.bullet(doc, "автоматически составлять расписание методом комбинаторной оптимизации с учётом всех "
    "жёстких и мягких ограничений — как для всего университета, так и для отдельного института "
    "с учётом ресурсов, уже занятых другими институтами;")
L.bullet(doc, "поддерживать черновики: строить предварительный вариант, публиковать его и откатывать;")
L.bullet(doc, "позволять править расписание вручную (добавлять, удалять и переносить занятия) с "
    "проверкой ограничений и обнаружением коллизий;")
L.bullet(doc, "отдавать расписание в трёх представлениях — по группам, преподавателям и аудиториям;")
L.bullet(doc, "управлять аудиторным фондом, оборудованием, доступностью и весами мягких ограничений;")
L.bullet(doc, "аутентифицировать пользователей через доменные учётные записи и разграничивать права;")
L.bullet(doc, "оповещать клиентов об изменениях расписания и нагрузки в реальном времени.")
L.body(doc, "Нефункциональные требования задают планку качества. Главное из них — корректность при "
    "одновременной работе: система обязана исключать коллизии и потерянные изменения, когда "
    "несколько сотрудников правят расписание параллельно, — именно этого не хватало прежнему "
    "процессу. К нему примыкают расширяемость (новое правило составления или источник данных не "
    "должны затрагивать существующий код), тестируемость ядра, отзывчивость интерфейса при "
    "длительной генерации и защита канала передачи учётных данных.")

L.heading2(doc, "2.2 Технологический стек")
L.body(doc, "Стек подобран под три приоритета — расширяемость, тестируемость и наличие зрелого "
    "решателя задач комбинаторной оптимизации. Основой служит платформа .NET и язык C#: "
    "статическая типизация и развитые средства абстракции упрощают поддержку крупной доменной "
    "модели, а у библиотеки Google OR-Tools, выбранной в разделе 1.4, есть готовый программный "
    "интерфейс для C# [13, 20]. Веб-приложение построено на ASP.NET Core — кроссплатформенном и "
    "высокопроизводительном каркасе [21, 22].")
L.body(doc, "Данные хранятся в PostgreSQL: эта СУБД с открытым исходным кодом поддерживает строгий "
    "уровень изоляции SERIALIZABLE, без которого невозможна заявленная защита от коллизий [27]. "
    "Доступ к ней организован через объектно-реляционное отображение Entity Framework Core с "
    "миграциями схемы [28]. Прикладной слой опирается на MediatR (шаблон «посредник» для "
    "разделения команд и запросов) и FluentValidation для декларативной проверки входных данных "
    "[25]. Снимок данных из системы «Нагрузка ВУЗа», развёрнутой на MS SQL, читается через "
    "микро-ORM Dapper; уведомления реального времени доставляет SignalR. Сводный состав стека "
    "приведён в таблице 2.1.")
L.table_caption(doc, "Таблица 2.1 — Технологический стек серверной части")
L.table(doc, [
    ["Категория", "Технология", "Назначение"],
    ["Язык и платформа", "C#, .NET, ASP.NET Core", "Серверное приложение и веб-API"],
    ["Доступ к данным", "Entity Framework Core, Npgsql", "ORM и драйвер PostgreSQL"],
    ["СУБД", "PostgreSQL", "Хранение данных, транзакции SERIALIZABLE"],
    ["Оптимизация", "Google OR-Tools (CP-SAT)", "Решатель задачи составления расписания"],
    ["Прикладной слой", "MediatR, FluentValidation", "CQRS-посредник и валидация"],
    ["Интеграция с ММИС", "Dapper, Microsoft.Data.SqlClient", "Чтение снимка справочников и нагрузки"],
    ["Реальное время", "SignalR", "Уведомления клиентов по WebSocket"],
    ["Аутентификация", "LDAP (служба каталогов), JWT", "Вход через доменные учётные записи"],
], aligns=[L.AL.LEFT, L.AL.LEFT, L.AL.LEFT], widths=[1.6, 2.2, 2.6])
L.body(doc, "Все перечисленные средства — зрелые технологии с открытым исходным кодом и активной "
    "поддержкой, что обеспечивает долговременную сопровождаемость системы.")

L.heading2(doc, "2.3 Архитектура серверной части")
L.body(doc, "Серверная часть построена по принципам чистой архитектуры (Clean Architecture) и "
    "предметно-ориентированного проектирования [23, 24] и разделена на четыре проекта: Domain, "
    "Application, Infrastructure и API. Деление подчинено правилу зависимостей — внешние слои "
    "знают о внутренних, но не наоборот; в центре находится слой предметной области, не зависящий "
    "ни от каких технологий. Схема слоёв и направление зависимостей показаны на рисунке 2.1.")
L.figure(doc, fig("04"), "Рисунок 2.1 — Многоуровневая архитектура серверной части")
L.body(doc, "Слой Domain содержит сущности и бизнес-правила. Сущности инкапсулированы: их состояние "
    "меняется только через фабричные методы и методы поведения, а инварианты проверяются "
    "статическим классом Guard, поэтому объект невозможно создать в некорректном состоянии. Слой "
    "Application реализует сценарии использования как команды и запросы (CQRS) и содержит "
    "алгоритмическое ядро, объявляя при этом интерфейсы к инфраструктуре. Слой Infrastructure "
    "реализует эти интерфейсы конкретными технологиями — доступ к базе через EF Core, интеграцию "
    "с ММИС, аутентификацию, фоновые службы. Слой API публикует REST-интерфейс и хаб уведомлений. "
    "Благодаря инверсии зависимостей конкретные технологии подключаются на внешнем слое и могут "
    "быть заменены, не затрагивая предметную логику, — это и есть техническая основа требований "
    "расширяемости и тестируемости.")
L.body(doc, "Разделение на команды и запросы выполнено посредником MediatR: каждый сценарий — это "
    "отдельный обработчик, а сквозная логика вынесена в конвейерное поведение, которое "
    "выполняется перед обработчиком. Так, проверка входных данных реализована единым поведением "
    "ValidationBehavior: оно прогоняет валидаторы FluentValidation до того, как команда дойдёт до "
    "обработчика, поэтому бизнес-логика всегда получает уже корректные данные.")

L.heading2(doc, "2.4 Модель базы данных")
L.body(doc, "Модель данных охватывает три предметные группы: структуру университета с аудиторным "
    "фондом, учебную нагрузку с календарём и собственно расписание с ограничениями. "
    "Проектирование выполнено в третьей нормальной форме, целостность поддерживается первичными и "
    "внешними ключами. Логическая модель приведена на рисунке 2.2.")
L.figure(doc, fig("05"), "Рисунок 2.2 — Логическая модель базы данных")
L.body(doc, "Организацию университета описывают институт (Institute), кафедра (Department) и "
    "преподаватель (Teacher), а учебные коллективы — ступень (Degree), курс (Course) и группа "
    "(Group). Аудиторный фонд образуют корпуса (Building), аудитории (Classroom) с вместимостью и "
    "оборудование (Equipment); оснащение аудитории задаёт связь «многие-ко-многим» EquipmentRoom. "
    "Ядро нагрузки — учебный план (Curriculum), связывающий преподавателя, дисциплину (Subject), "
    "учебный поток (AcademicStream) и тип занятия и хранящий по-нагрузочные признаки: возможность "
    "параллельного проведения, необходимость двойной пары и предпочтительный корпус. Объём "
    "нагрузки задаётся на семестр (SemesterWorkload) и на неделю (WeekWorkload).")
L.body(doc, "Календарь складывается из семестра (Semester), недель (Week) с типом чередования и "
    "учебных дней (WeekDay) с временными слотами (TimeSlot). Само расписание — это занятие "
    "(Lesson), связывающее поток, аудиторию, слот и учебный план; группы входят в поток через "
    "связку StreamGroups. Конфигурацию ограничений вынесли в отдельные сущности: сетки "
    "доступности (TeacherAvailability, ClassroomAvailability), требуемое дисциплиной оборудование "
    "(NeededEquipment) и веса мягких ограничений (ConstraintConfig). Историю нагрузки хранят "
    "журналы SemesterLog и WeekLog. Назначение ключевых сущностей сведено в таблицу 2.2.")
L.table_caption(doc, "Таблица 2.2 — Ключевые сущности базы данных")
L.table(doc, [
    ["Сущность", "Назначение"],
    ["Institute, Department, Teacher", "Организационная структура и преподаватели"],
    ["Degree, Course, Group", "Ступени образования, курсы и учебные группы"],
    ["Building, Classroom, Equipment, EquipmentRoom", "Аудиторный фонд и его оснащение"],
    ["Curriculum, Subject, AcademicStream", "Учебный план: дисциплина, поток, тип, признаки"],
    ["SemesterWorkload, WeekWorkload", "Плановая нагрузка на семестр и на неделю"],
    ["Semester, Week, WeekDay, TimeSlot", "Календарь и сетка временных слотов"],
    ["Lesson", "Занятие расписания (черновик или опубликованное)"],
    ["TeacherAvailability, ClassroomAvailability", "Сетки доступности по дням и парам"],
    ["NeededEquipment, ConstraintConfig", "Требуемое оборудование и веса мягких ограничений"],
    ["SemesterLog, WeekLog", "Журнал изменений учебной нагрузки"],
], aligns=[L.AL.LEFT, L.AL.LEFT], widths=[3.1, 3.3])
L.body(doc, "Сопоставление доменных классов с таблицами задано конфигурациями EF Core, а развитие "
    "схемы фиксируется миграциями — это делает структуру базы версионируемой, а её развёртывание "
    "воспроизводимым.")

L.heading2(doc, "2.5 Математическая модель задачи составления расписания")
L.body(doc, "Постановку задачи из раздела 1.3 доведём до полной математической модели, которую "
    "решает CP-SAT. Пусть W — множество единиц семестровой нагрузки, R — множество аудиторий, "
    "T — множество временных слотов семестра; для преподавателя p через Wp обозначим его "
    "нагрузки, для группы g через Wg — нагрузки, в которых она участвует. Каждой тройке "
    "«нагрузка — аудитория — слот» сопоставляется булева переменная (формула 2.1):")
L.formula(doc, "xwrt ∈ {0, 1},   w ∈ W,   r ∈ R,   t ∈ T,", "2.1")
L.body(doc, "Переменная равна 1 тогда и только тогда, когда нагрузка w проводится в аудитории r в "
    "слоте t. Часть троек недопустима заведомо — по вместимости, оборудованию или смене; для них "
    "переменная не создаётся вовсе. Этот приём отсечения (прунинг) кратно сокращает размер "
    "модели и подробнее разобран в разделе 2.6.")
L.body(doc, "Точное соответствие объёму нагрузки. Число назначенных по каждой нагрузке занятий "
    "равно плановому числу пар — часам Hw, делённым на два, поскольку пара соответствует двум "
    "академическим часам (формула 2.2):")
L.formula(doc, "∑(r∈R) ∑(t∈T) xwrt = Hw ⁄ 2,   ∀ w ∈ W.", "2.2")
L.body(doc, "Запрет пересечений по ресурсам. В одной аудитории в один слот идёт не более одного "
    "занятия (2.3); преподаватель не ведёт более одного занятия одновременно (2.4); группа не "
    "находится более чем на одном занятии одновременно (2.5):")
L.formula(doc, "∑(w∈W) xwrt ≤ 1,   ∀ r ∈ R,  t ∈ T,", "2.3")
L.formula(doc, "∑(w∈Wp) ∑(r∈R) xwrt ≤ 1,   ∀ p,  t ∈ T,", "2.4")
L.formula(doc, "∑(w∈Wg) ∑(r∈R) xwrt ≤ 1,   ∀ g,  t ∈ T.", "2.5")
L.body(doc, "Оборудование и вместимость. Нагрузку нельзя назначить в аудиторию без нужного "
    "оборудования и в аудиторию, которая меньше потока. Обозначив через Eq(w) и Eq(r) требуемое и "
    "имеющееся оборудование, через c(r) — вместимость, через n(w) — число студентов потока, "
    "получаем формулы 2.6 и 2.7:")
L.formula(doc, "xwrt = 0,   если  Eq(w) ⊄ Eq(r),   ∀ t ∈ T,", "2.6")
L.formula(doc, "xwrt = 0,   если  c(r) < n(w),   ∀ t ∈ T.", "2.7")
L.body(doc, "Смена обучения. Занятия группы размещаются только в слотах её смены: для первой "
    "допустимы пары 1–4, для второй — 5–6, для вечерней — с 7-й. Обозначив через Tsh(w) "
    "допустимые слоты, получаем формулу 2.8:")
L.formula(doc, "xwrt = 0,   если  t ∉ Tsh(w),   ∀ r ∈ R.", "2.8")
L.body(doc, "Двойные занятия. Лабораторные и подобные им нагрузки идут спаренно — ровно двумя "
    "смежными слотами в одной аудитории. Это задаётся двумя условиями: занятие в слоте требует "
    "занятия в соседнем (нет одиночных), а в любых трёх подряд слотах занято не более двух (нет "
    "серий из трёх и более), — формула 2.9:")
L.formula(doc, "xwr,t ≤ xwr,t−1 + xwr,t+1 ;   xwr,t−1 + xwr,t + xwr,t+1 ≤ 2.", "2.9")
L.body(doc, "Мягкие ограничения входят в целевую функцию штрафами. Доступность задаётся знаковым "
    "весом слота: предпочтительные слоты получают отрицательный вес (бонус), нежелательные и "
    "запрещённые — положительный, что позволяет избегать неудобного времени, не делая модель "
    "неразрешимой. «Окно» штрафуется за каждый пустой слот, окружённый занятиями того же субъекта "
    "в течение дня. Превышение дневного лимита пар (для группы — 4, для преподавателя — 7) "
    "штрафуется пропорционально числу лишних пар; лимит сделан мягким с большим штрафом, ведь в "
    "исключительных случаях превышение неизбежно. Дополнительно штрафуются занятия вне "
    "предпочтительного корпуса, перемещение между корпусами в смежных слотах и недостаточная "
    "параллельность подгрупп. Качество расписания формализует целевая функция (формула 2.10):")
L.formula(doc, "F(x) = ∑(k∈K) pk · nk(x) → min,", "2.10")
L.body(doc, "где K — множество учитываемых мягких ограничений, pk — весовой коэффициент k-го "
    "ограничения, nk(x) — число его нарушений, выраженное через переменные модели. Веса штрафов "
    "за «окна» и доступность хранятся в базе (сущность ConstraintConfig) и настраиваются "
    "администратором; остальные веса заданы в конфигурации решателя. Решателю ставится задача "
    "найти назначение переменных, удовлетворяющее жёстким ограничениям (2.2)–(2.9) и "
    "минимизирующее (2.10).")

L.heading2(doc, "2.6 Модуль составления расписания")
L.body(doc, "Чтобы добавление нового правила не затрагивало готовый код, модуль составления "
    "расписания построен по шаблону «Строитель» [33]. Каждая часть модели — переменные, отдельное "
    "жёсткое ограничение или слагаемое целевой функции — это самостоятельный строитель за единым "
    "интерфейсом IModelSectionBuilder. Распорядитель сборки ScheduleModelDirector применяет "
    "строители к модели в строгом порядке: сначала создаются переменные, затем жёсткие "
    "ограничения, далее штрафы и в конце фиксируется целевая функция. Структура модуля показана "
    "на рисунке 2.3.")
L.figure(doc, fig("06"), "Рисунок 2.3 — Архитектура модуля составления расписания")
L.body(doc, "Так реализуется принцип открытости/закрытости: новое правило — это новый "
    "класс-строитель, включаемый в конвейер, а не правка существующих. На этапе создания "
    "переменных выполняется прунинг: тройки, заведомо запрещённые по вместимости, оборудованию и "
    "смене, отбрасываются и переменных для них не возникает — на реальных данных это в разы "
    "сокращает и число переменных, и число ограничений. Готовую модель получает решатель CP-SAT, "
    "обёрнутый отдельным компонентом: он задаёт параметры поиска (по умолчанию лимит времени "
    "180 секунд и 8 параллельных потоков), запускает решение и извлекает результат — назначения "
    "переменных, статус и метрики (значение целевой функции и время работы).")
L.body(doc, "Для крупного университета задача на весь семестр велика, поэтому помимо генерации для "
    "вуза целиком предусмотрена декомпозиция по институтам. При генерации одного института "
    "ресурсы — аудитории и преподаватели, — уже занятые расписанием ранее обработанных институтов "
    "того же семестра, передаются в модель как дополнительные жёсткие блокировки, что исключает "
    "конфликты между институтами. Одновременно из расписания прошлого семестра формируется набор "
    "«якорей» — мягких предпочтений по корпусам и слотам, удерживающих новое расписание близким к "
    "привычному. Декомпозиция уменьшает каждую подзадачу и повышает практическую разрешимость.")
L.body(doc, "Поскольку поиск может длиться до нескольких минут, генерация выполняется асинхронно. "
    "Контроллер лишь кладёт задание в очередь и сразу возвращает его идентификатор и код 202; "
    "фоновая служба извлекает задания и выполняет генерацию вне потока обработки HTTP-запросов, а "
    "клиент периодически опрашивает статус задания. Длительная генерация при этом не блокирует "
    "интерфейс. Последовательность асинхронной генерации показана на рисунке 2.4.")
L.figure(doc, fig("07"), "Рисунок 2.4 — Последовательность асинхронной генерации расписания")

L.heading2(doc, "2.7 Версионирование расписания и управление черновиками")
L.body(doc, "Расписание составляют итеративно: сотрудник строит предварительный вариант, проверяет "
    "его и при необходимости перестраивает, прежде чем сделать общедоступным. Поэтому у каждого "
    "занятия есть признак версии — черновик (Draft) или опубликованное (Current). И "
    "сгенерированные автоматически, и добавленные вручную занятия сначала становятся черновиком и "
    "видны только составителю. Жизненный цикл версий показан на рисунке 2.5.")
L.figure(doc, fig("08"), "Рисунок 2.5 — Жизненный цикл версий расписания")
L.body(doc, "Публикация переводит занятия института из черновика в опубликованные, заменяя прежнее "
    "расписание. Отмена удаляет только черновики, оставляя опубликованное нетронутым, и потому "
    "идемпотентна. Перегенерация института сначала удаляет его прежний вариант и создаёт новый "
    "черновик. Все эти операции изменяют состояние и потому выполняются под защитой транзакций — "
    "о ней следующий раздел.")

L.heading2(doc, "2.8 Транзакционная защита от коллизий")
L.body(doc, "Невозможность безопасной совместной работы была одной из главных бед прежнего "
    "процесса: при одновременном редактировании общих файлов возникали наложения занятий и "
    "потерянные изменения. Поэтому все операции записи расписания выполняются в транзакциях с "
    "уровнем изоляции SERIALIZABLE — самым строгим по стандарту SQL [27, 29]. На этом уровне СУБД "
    "гарантирует, что результат параллельных транзакций эквивалентен какому-то их "
    "последовательному выполнению, а при конфликте сериализации одна из транзакций отменяется. "
    "Чтобы отмена оставалась незаметной для пользователя, прерванная транзакция автоматически "
    "повторяется стратегией выполнения.")
L.body(doc, "Особенно важна такая защита при ручном добавлении занятия, минующем решатель. Здесь "
    "система сначала проверяет отсутствие коллизий по аудитории, преподавателю и группам, а затем "
    "сохраняет занятие. Без должной изоляции две параллельные операции могли бы одновременно "
    "пройти проверку и создать конфликтующие занятия — классическое состояние гонки «проверка, "
    "затем запись». Объединение проверки и вставки в одну сериализуемую транзакцию исключает "
    "такую возможность. При этом длительная работа решателя вынесена за пределы транзакции: внутри "
    "неё выполняется лишь короткая запись результата, поэтому блокировки не удерживаются на время "
    "поиска решения.")

L.heading2(doc, "2.9 Журналирование изменений учебной нагрузки")
L.body(doc, "Чтобы сотрудники бюро не сверяли нагрузку вручную, система ведёт журнал её изменений "
    "на двух уровнях — семестровом (SemesterLog) и недельном (WeekLog). Каждая запись фиксирует "
    "тип операции (добавление, изменение, удаление), прежнее и новое значение часов и отметку "
    "времени. Журналирование встроено в доменную модель: запись появляется только при фактическом "
    "изменении значения часов, поэтому «пустых» событий в истории не возникает. При удалении "
    "нагрузки её запись в журнале сохраняется — внешний ключ обнуляется, а сама запись остаётся, "
    "поэтому история всегда полна. Накопленный журнал служит и источником истории в интерфейсе, и "
    "основой уведомлений реального времени.")

L.heading2(doc, "2.10 Интеграция с системой «Нагрузка ВУЗа»")
L.body(doc, "Сведения о нагрузке, преподавателях, группах и дисциплинах поставляет система ММИС с "
    "модулями «Деканат» (справочники) и «Нагрузка ВУЗа» (часы), развёрнутая на сервере MS SQL. "
    "Интеграция реализована как периодическая фоновая синхронизация по схеме «извлечение — "
    "преобразование — загрузка» (ETL). Конвейер синхронизации показан на рисунке 2.6.")
L.figure(doc, fig("09"), "Рисунок 2.6 — Конвейер интеграции с системой «Нагрузка ВУЗа»")
L.body(doc, "Компонент чтения снимает из базы ММИС полный срез справочников и нагрузки только "
    "запросами на выборку, не трогая исходные данные. Служба синхронизации переносит справочники "
    "идемпотентно: для каждой записи по её коду ММИС детерминированно вычисляется идентификатор, "
    "по которому выполняется вставка-или-обновление, поэтому повторные прогоны не плодят дубликатов. "
    "Нагрузка синхронизируется с вычислением разницы: новые строки помечаются как добавление, "
    "изменённые часы — как изменение, пропавшие в источнике — как удаление, и каждое такое "
    "событие попадает в журнал. Вся синхронизация идёт в одной сериализуемой транзакции. После "
    "переноса достраивается календарная сетка — рабочие дни и пары новых недель, которых в ММИС "
    "нет, но которые нужны решателю как ось «время». Запускает синхронизацию фоновая служба по "
    "расписанию — ежедневно или с заданным интервалом. Вся интеграция спрятана за интерфейсами "
    "прикладного слоя, поэтому источник данных можно заменить, не трогая остальную систему.")

L.heading2(doc, "2.11 Программный интерфейс (REST API)")
L.body(doc, "Клиент общается с серверной частью в стиле REST: ресурсы адресуются единообразными "
    "идентификаторами, операции выполняются методами HTTP, данные передаются в формате JSON [26]. "
    "Интерфейс сгруппирован по предметным областям; основные группы ресурсов сведены в таблицу 2.3.")
L.table_caption(doc, "Таблица 2.3 — Основные группы ресурсов программного интерфейса")
L.table(doc, [
    ["Группа ресурсов", "Назначение и операции"],
    ["lessons", "Чтение по преподавателю/группе/аудитории, добавление, удаление и перенос занятий, фоновая генерация, публикация и сброс черновика"],
    ["workloads, workloads/changes", "Учебная нагрузка и журнал её изменений (с пагинацией)"],
    ["institutes, departments, teachers, groups", "Справочники университета для каскадных фильтров"],
    ["buildings, rooms, classrooms, equipments", "Аудиторный фонд и оборудование (CRUD)"],
    ["availability, constraints", "Доступность преподавателей/аудиторий и веса ограничений"],
    ["semesters, weeks, timeslots", "Календарь и сетка слотов"],
    ["auth/login", "Аутентификация через доменные учётные записи"],
], aligns=[L.AL.LEFT, L.AL.LEFT], widths=[2.2, 4.2])
L.body(doc, "Запросы, отдающие большие объёмы (нагрузка, история), поддерживают серверную "
    "постраничную выдачу. Ошибки возвращаются в стандартном формате ProblemDetails: попытка "
    "создать конфликтующее занятие даёт код 409 с описанием обнаруженных коллизий, а ошибка "
    "валидации — код 400. Интерфейс описан спецификацией OpenAPI с интерактивной страницей "
    "Swagger, что упрощает его освоение разработчиком клиента.")

L.heading2(doc, "2.12 Аутентификация, авторизация и уведомления реального времени")
L.body(doc, "Поскольку у всех сотрудников есть доменные учётные записи, на них и построена "
    "аутентификация. При входе серверная часть выполняет привязку (bind) к службе каталогов "
    "Active Directory по протоколу LDAP, проверяя логин и пароль, затем читает членство "
    "пользователя в группах и проверяет принадлежность к группе доступа [31]. При успехе "
    "выпускается подписанный маркер JSON Web Token (JWT) с именем пользователя и его ролями, "
    "который клиент предъявляет в последующих запросах [30]. Последовательность входа показана на "
    "рисунке 2.7.")
L.figure(doc, fig("10"), "Рисунок 2.7 — Аутентификация через доменные учётные записи")
L.body(doc, "Все ресурсы интерфейса по умолчанию требуют действительного маркера — исключение лишь "
    "у операции входа. Роли, перенесённые из доменных групп в маркер, разграничивают права "
    "администратора и сотрудника бюро. Маркеры живут ограниченное время и передаются по "
    "защищённому каналу.")
L.body(doc, "Чтобы сотрудники сразу видели правки друг друга, серверная часть поддерживает "
    "уведомления реального времени на основе SignalR поверх постоянного соединения WebSocket [32]. "
    "После входа клиент подключается к хабу уведомлений; при публикации расписания института или "
    "при изменении нагрузки по итогам синхронизации с ММИС сервер рассылает подключённым клиентам "
    "соответствующие события, и их интерфейсы обновляются без перезагрузки страницы. Подключение к "
    "хабу, как и обращения к API, требует действительного маркера. Так обеспечивается "
    "согласованная совместная работа и снимается нужда в ручной сверке данных.")
L.body(doc, "Спроектированные в этой главе архитектура, модель данных, математическая модель и "
    "подсистемы образуют целостное техническое решение; его программная реализация и тестирование "
    "рассматриваются в следующей главе.")

# ---- переместить новые элементы перед главой 3 ----
# python-docx добавляет новые элементы в конец body, но перед завершающим <w:sectPr>.
# Поэтому новые элементы — это срез [n_before-1 : -1] (если последний — sectPr) или хвост.
children = list(body)
tail_is_sectpr = bool(children) and children[-1].tag == qn('w:sectPr')
start = n_before - 1 if tail_is_sectpr else n_before
end = len(children) - 1 if tail_is_sectpr else len(children)
new_els = children[start:end]
for ne in new_els:
    el_ch3.addprevious(ne)

doc.save("Диплом.docx")
print("OK: глава 2 переписана, элементов добавлено:", len(new_els))
