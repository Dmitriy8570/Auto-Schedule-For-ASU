# Оставшиеся задачи

> Состояние на 2026-06-22. Продуктовый код (треки A–E) практически закрыт: бэкенд и
> фронтенд собираются, основной функционал работает end-to-end, 43 быстрых теста зелёные.
> Ниже — только то, что осталось доделать.

## 1. Диплом (ВКР) — главное и самое объёмное

Ведётся в `Docs/диплом/` (см. `Прогресс-диплома.md`). Не начато:
- [ ] Глава 2 — проектирование (требования, стек, архитектура, БД+ER, транзакции,
  версионирование, журнал нагрузки, REST API, математическая модель ограничений,
  ММИС, аутентификация, real-time).
- [ ] Глава 3 — реализация и тестирование.
- [ ] Заключение.
- [ ] Реферат (1 страница после титула).
- [ ] Библиографический список (30+ источников по ГОСТ).

## 2. Тюнинг солвера под масштаб (опционально / исследовательское)

- [ ] На большом инстансе (~530 нагрузок) CP-SAT за 180 с возвращает `Unknown` (не успевает).
  Прунинг убрал краш по памяти, но полное решение такого размера за лимит времени —
  отдельная задача: подбор параметров поиска, эвристики первого решения, либо декомпозиция
  на меньшие подзадачи. На реальных данных через docker можно прогнать с бóльшим лимитом.

## 3. Фронтенд — полировка (P2, по желанию)

- [ ] `vue-router` вместо ручного переключения вкладок (+ состояние в URL).
- [ ] Общий стор (Pinia) для справочников и сессии (не перезагружать списки на каждой вкладке).
- [ ] Предупреждение об истечении сессии (401 / `expiresAtUtc`).
- [ ] Компонентные/e2e-тесты ключевых сценариев (логин, нагрузка, генерация).

## 4. Бэкенд — добор тестов (трек D, мелочь)

- [ ] Хендлер-тест успешной генерации по институту (сейчас покрыт только Infeasible-путь).
- [ ] Тесты эндпоинтов трека A (доступность преподавателя/аудитории, по-нагрузочные правила, смена).
