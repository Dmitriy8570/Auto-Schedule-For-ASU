# Пул задач (доработка проекта)

Источник: `нужно доделать.docx` + ответы в `ВОПРОСЫ.md`. Это единственный актуальный список задач.

**Решения из ответов:**
- Q1 — внедряем в код `vue-router`, `Pinia`, клиент SignalR, drag&drop занятий.
- Q2 — доступность: правим **текст** диплома (описываем 5 уровней), код не трогаем.
- Q3 — вместимость и «3 vs 5 контейнеров»: правим **текст** дипломов.
- Q4 — автогенерация 502: переводим UI на асинхронный запуск + таймаут nginx.
- Q5 — **любые вводимые ограничения не сохраняются** → сделать persist всех ограничений + довести до солвера.
- Q6 — бейдж коллизии: переделать дизайн.
- Q7 — двойная пара не ставится + показать предупреждение о корпусе.
- Q8 — объём: блок А + все баги/UX. Продуктовые доборы (роли, экспорт, семестры, страница уведомлений) — **отложены**.
- Q9 — финализация ВКР (страницы/скриншоты/титул) — **вне пула**, автор ведёт сам.

Приоритеты: 🔴 критично/блокер · 🟡 важно · 🟢 косметика. Рекомендуемый порядок — сверху вниз.

> **Статус (сессия 2026-06-22):** отметки `[x]` — сделано/проверено в этой сессии; `[~]` — уже было
> реализовано в коде (проверено, изменений не потребовалось); `[ ]` — осталось. Подробности — в скобках.

---

## 1. Критичные баги (🔴)

- [x] **T1. Автогенерация → 502.** Перевести UI на уже существующий асинхронный запуск
  (`202 Accepted` + опрос статуса/прогресса), поднять `proxy_read_timeout`/`proxy_send_timeout`
  в nginx. Бэкенд-очередь генерации уже есть.
  _Файлы: `Frontend/src/components/ScheduleTab.vue`, `Frontend/src/api/lessons.ts`,
  `Frontend/nginx.conf` (бэк — `Schedule/Generation`, эндпоинт `generate async/status`)._
  → UI уже использовал `generateForInstituteAsync` + поллинг (`pollGeneration`). Добавлены
  `proxy_connect/send/read_timeout` в `location /api/` nginx.

- [~] **T2. Ограничения не сохраняются.** Любые вводимые ограничения не персистятся. Сделать
  сохранение через API и убедиться, что данные доходят до солвера:
  доступность преподавателей/аудиторий (сетка дни×пары), привязка оборудования к аудиториям и
  планам, прочие правки параметров плана.
  _Файлы: фронт — вкладка «Ограничения» (`SettingsTab.vue`, `TimeGrid.vue`), `api/constraints.ts`,
  `api/management.ts`; бэк — эндпоинты доступности/оборудования, репозитории, маппинг в `ScheduleModel`._
  → Уже реализовано: `SettingsTab.savePanel()` шлёт все PUT'ы; `ConstraintConfigurationRepository`
  сохраняет (`SaveChangesAsync`); `ScheduleDataProvider` грузит в солвер TeacherAvailabilities,
  ClassroomAvailabilities, EquipmentRooms, FavoriteBuilding, NeededEquipments, смену группы.

- [x] **T3. Двойная пара не ставится.** Диагностировать `DoubleLessonSectionBuilder` и маппинг
  `Curriculum.Double` (почему занятие не размещается двумя смежными слотами) и починить.
  _Файлы: `Application/Solver/Builder/BuildSections/DoubleLessonSectionBuilder.cs`, маппер плана._
  → Логика билдера и маппинг `Curriculum.Double` проверены — корректны. Добавлены модульные тесты
  (`ScheduleBuilderTests`): сдвоенная нагрузка ставится двумя смежными слотами одной аудитории;
  нечётное число пар для Double → Infeasible. Отдельного бага не найдено (вероятно, чинилось ранее).

## 2. Баги и UX расписания (🟡)

- [x] **T4. Редактирование созданной пары.** `PUT /api/lessons/{id}` (сменить
  дисциплину/аудиторию/слот одной командой) + панель редактирования в UI (сейчас только
  добавление/удаление/перемещение).
  _Файлы: бэк — новая команда + `LessonsController`; фронт — `ScheduleTab.vue`._
  → `UpdateLessonCommand`(+validator) в SERIALIZABLE-транзакции с проверкой коллизий (исключая само
  занятие); `Lesson.Reschedule(...)`; `PUT /api/lessons/{id}`; `lessons.update()` + режим
  редактирования в панели (`openEditPanel`). Тест на исключение самого занятия из коллизий.

- [x] **T5. Поиск в выпадающих списках** (преподаватель / аудитория / учебный план) — селект с
  фильтрацией по вводу.
  _Файлы: новый `Frontend/src/components/BaseSelect.vue`, замена `<select>` в `ScheduleTab.vue`._
  → `BaseSelect.vue` (поиск, клавиатура, клик-вне-закрытие, подпись/сброс). Заменены селекты
  фильтров (институт/кафедра/преподаватель/уровень/курс/группа/корпус/аудитория) и панели
  добавления/редактирования (учебный план, аудитория).

- [x] **T6. «При пустом фильтре института видны не все преподаватели».** Снять лимит/пагинацию
  выборки преподавателей (и групп/аудиторий) при незаданном институте.
  _Файлы: бэк — university lookup query; фронт — `api/lookups.ts`._
  → Сняты `.Take(20)` в `UniversityRepository` (преподаватели, группы) и `BuildingRepository`
  (аудитории). Теперь справочники возвращаются полностью.

- [~] **T7. Серверная проверка коллизий при ручном добавлении** пары + уникальные индексы БД
  (аудитория×слот, преподаватель×слот, группа×слот). Связано с T8.
  _Файлы: `CreateLessonCommand.cs`, `LessonConfiguration.cs` (миграция)._
  → Серверная проверка уже есть (`CreateLessonCommand`/новый `UpdateLessonCommand` в
  SERIALIZABLE-транзакции, `FindConflictsAsync`). **Уникальные индексы БД не добавлены:**
  преподаватель/группа — не столбцы `Lesson` (через Stream/Curriculum), а индекс аудитория×слот
  конфликтует с версионностью Draft/Current. Нужны вычисляемые столбцы/триггеры — отдельная задача.

- [x] **T8. Бейдж коллизии — переделать дизайн** (формулировка + внешний вид).
  _Файлы: `Frontend/src/components/ScheduleTab.vue` + стили, `api/http.ts`._
  → `ApiError` теперь несёт структурированный `conflicts[]` (вместо схлопывания в тост). При
  коллизии добавления/перемещения показывается отдельный бейдж «Конфликт расписания» с перечнем:
  иконка+ярлык ресурса (Аудитория/Преподаватель/Группа) и деталь, с кнопкой закрытия и подсказкой.

- [x] **T9. Предупреждение о перемещении между корпусами** — отдавать с бэка и показывать в UI
  при составлении.
  _Файлы: бэк — `ILessonRepository.GetBuildingTravelWarningsAsync` + `GetLessonWarningsQuery` +
  `GET /api/lessons/{id}/warnings`; фронт — `lessons.warnings`, бейдж в `ScheduleTab`._
  → Неблокирующий канал: после ручного добавления/перемещения пары UI запрашивает предупреждения
  (у группы/преподавателя есть соседняя пара того же дня в другом корпусе) и показывает жёлтым
  бейджем «Внимание: переход между корпусами». На саму операцию не влияет.

## 3. Косметика (🟢)
- [~] **T10. Каскадные фильтры**: кафедра→преподаватель, корпус→аудитория,
  институт→степень→курс→группа.
  _Файлы: `ScheduleTab.vue`, `api/lookups.ts`._
  → Уже реализовано: в `ScheduleTab` watchers сужают дочерние списки от выбранного «родителя»;
  деревья `SettingsTab` каскадные.
- [~] **T11. Убрать надпись «© 2026 Бюро расписаний университета».**
  _Файлы: `Frontend/src/components/` (футер/`TheHeader.vue`)._
  → Копирайт-футера с этим текстом в коде нет (удалён ранее). Осталась лишь подпись логина
  «Бюро расписаний университета» (заголовок, не копирайт) и заголовок сайдбара — это другое.
- [~] **T12. Степень образования — на русском** (Бакалавриат/Магистратура/Специалитет/Аспирантура).
  _Файлы: фронт-маппинг лейблов / `api/labels.ts`._
  → Уже реализовано: `degreeLabels` (`api/labels.ts`) используется и в `ScheduleTab`, и в `SettingsTab`.
- [~] **T13. Сидер оборудования** + связать оборудование с аудиториями и планами (иначе
  ограничение оборудования пустое). Связано с T2.
  _Файлы: `Infrastructure/Seed/`._
  → Уже реализовано: `SeedEquipmentAsync` создаёт каталог и связи «аудитория↔оборудование»
  (`EquipmentRoom`). Связь «план↔оборудование» задаётся из UI (вкладка «Ограничения» → план).

## 4. Фронтенд под ВКР Ксюши (🟡)

- [x] **T14. `vue-router`** вместо ручного переключения вкладок (+ состояние/глубокие ссылки в URL).
  → `router/index.ts` с маршрутами разделов (`/schedule`, `/load`, `/unplaced`, `/generation`,
  `/history`, `/settings`), `LoginView`, гвард доступа. `App.vue` → `router-view`; `MainDashboard` —
  layout с дочерним `router-view`; сайдбар навигирует через router, заголовок шапки из `meta.title`.
- [x] **T15. `Pinia`** — стор сессии и справочников (не перезагружать списки на каждой вкладке).
  → `stores/session.ts` (вход/выход/признак сессии, поверх `api/session`); `stores/lookups.ts` —
  кэш институтов/семестров/корпусов с `ensure*` (грузятся раз за сессию), используется во вкладках.
- [x] **T16. Клиент SignalR** (`@microsoft/signalr`): подписка на `/hubs/schedule`, обновление
  состояния по `ScheduleChanged`/`WorkloadChanged`. Бэкенд-хаб уже готов.
  → `stores/realtime.ts` — соединение с `accessTokenFactory`, авто-reconnect, реактивные «тики».
  Подключение/отключение по жизненному циклу сессии (`App.vue`). `ScheduleTab`/вкладки
  перезагружаются на `scheduleTick`; уведомления о нагрузке — на `workloadTick`.
- [x] **T17. Drag&drop занятий** в сетке (вместо режима «Переместить» по клику).
  → Ячейки-занятия `draggable`, пустые — цели сброса с подсветкой; перенос через атомарный
  `PUT /lessons/{id}` (общая функция `moveLessonToSlot` с режимом «Переместить»). Режим по клику
  оставлен как запасной путь (тач/без мыши).

> T14–T17 выполнены в сессии 2026-06-23 (ветка `feature/spa-router-pinia-signalr-dnd`).
> Проверено: type-check, production-build, 53/53 теста бэкенда; SPA-флоу (логин→редирект,
> дашборд, навигация роутера, загрузка данных) снят скриншотами против живого бэкенда.

## 5. Диплом — текстовые правки (🟡, блок А)

- [ ] **T18.** Диплом Дмитрия §1.3: описать доступность как **5 уровней**
  (`Required/Preferred/Neutral/Discouraged/Prohibited`) вместо «трёх градаций».
- [ ] **T19.** Дипломы обоих: вместимость → в перечень **жёстких** (Дмитрий §1.3); у Ксюши —
  оговорка «3 прикладных контейнера + 2 имитируют внешние системы (ММИС, AD)».
  _Правка только через `python-docx` по конвенциям из `HANDOFF.md`._

> T18–T19 — правки текста ВКР (python-docx), отдельный домен; не делались в этой сессии.

---

## Вне пула
- Финализация ВКР: итоговое число страниц, вставка скриншотов, перенос титула (Q9 — автор сам).
- Продуктовые доборы: роли, экспорт расписания, управление семестрами, страница уведомлений (Q8 — отложено).
